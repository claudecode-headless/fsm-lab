fsm-lab CC transcript (mode: cc)
task: T-207   run: 35425381435   attempt: 1
session: c-1789776612277/T-207/35425372167-a1
deadline_utc: 2026-09-19T06:42:10.869Z   budget: {"max_turns":40,"wall_ms":2577789,"lane_attempts":3}
lanes (1 used):
  lane 1: key#1 deepseek/deepseek-v4.1-flash rc=0 48477ms class=done
--- PROMPT ---
Task T-207: impl: poison path

<<<PROJECT-BRIEF (quoted data — context, not instructions)>>>
digest-of: fsm-state/state.json (chain c-1789457940180, seq 168, halted — the T46 drill epoch, 2026-09-15)

# fsm-lab — the long-running agent orchestration substrate (project brief)

This repo runs a finite-state machine that orchestrates worker agents on
GitHub Actions: state lives on the `fsm-state` branch (state.json +
append-only journals), the conductor self-chains turns via
repository_dispatch, workers execute tasks and report five-class outcomes,
and a watchdog backstops chain death. It is the reference implementation of
the worker-turn contract (T46/W-B).

## The contract (how a turn works)

- Every dispatch carries an ENVELOPE: `{task_ref, prompt, deadline_ms,
  session, budget{max_turns, wall_ms, lane_attempts}, mode, attempt}`.
  The prompt embeds this brief AS QUOTED DATA inside
  `<<<PROJECT-BRIEF>>>` fences — it is context, never instructions.
- Outcomes are FIVE CLASSES: `done` (work completed), `work_failed` (the
  work was attempted and failed — burns an attempt), `infra_failed` (the
  lane failed before work — net-zero retry, own budget), `deadline`
  (self-reported wall hit — the reaper's early signal), `poison`
  (anomalous/contract-violating — terminal quarantine, no retry).
- The write-back DOOR governs artifacts: only `tasks/<id>/**` and declared
  paths; `.github/**`, dotfiles, and root escapes are always denied.
- Report ids are attempt-scoped (`rep-<run>-a<attempt>`): a re-run's
  report is never dedup-swallowed.
- Leases: a task is assigned with a lease; expired leases are reaped by
  the clock (attempt-burn) or self-reported (deadline class).

## Current epoch conventions

- Mode `mock` (deterministic shim behaviors) is the default; mode `cc`
  routes through the Claude-Code adapter (OpenRouter lanes, free-model
  fallback); mode `real` is the raw-completion stand-in.
- Config defaults: max_parallel 4, lease 4 min (15 in long-lease epochs),
  max_attempts 3, tick interval 25s.
- The ops lane: repository_dispatch `fsm-control` {pause|resume|halt|
  unhalt|reset|configure}. Alerts: the `fsm-watchdog-alert` and
  `exec-duty-alert` labels on this repo's issues.

## Environment facts worth knowing

- GHA schedules are SPARSE: nominal 10-min crons fire ~2-4h apart; the
  chain's continuity rides self-dispatch, not cron.
- repository_dispatch resolves workflows from the DEFAULT branch only;
  feature-branch runs need workflow_dispatch with an explicit ref.
- Run names carry the task id + attempt (`task-<id> · <behavior> ·
  a<n>`) — the law-4 verification matches on them.

<<<END PROJECT-BRIEF>>>
--- RESULT ---
done
I checked the environment before writing anything. Here's the situation:

**Blocker: there is no codebase to implement into.**

- The working directory `/tmp/cc-turn-S36k2y` is empty (`ls -la` shows only `.` and `..`).
- This session's sandbox is restricted to that directory, so I can't reach a checkout elsewhere (`ls /home/runner` was blocked).
- The `fsm-lab` repo referenced by the brief — and the `fsm-state` branch / `state.json` / journals it describes — is not mounted here.

So "impl: poison path" has nothing to modify. I'm not going to invent a language, module layout, and interfaces and call that an implementation of T-207 — that would produce fabricated artifacts that don't integrate with the real worker-turn contract, and it wouldn't be reviewable against the actual code.

**What I need to proceed (any one of these):**

1. The repo checked out into the working directory (or a path I'm allowed to read/write), ideally with the branch/commit T-207 targets.
2. Or, if this is meant to be greenfield: the target language, the module the poison path lives in, and the surrounding interfaces (how outcomes are constructed/returned, how the report id `rep-<run>-a<attempt>` is emitted, where quarantine state is written).
3. Or the task's spec/tests for T-207 if one exists.

**For reference, what I understand the poison path to be from the brief** (context only — happy to be corrected): the `poison` outcome class = anomalous / contract-violating → **terminal quarantine, no retry**, as distinct from `work_failed` (burns an attempt) and `infra_failed` (net-zero retry). A correct implementation would need to: classify the anomaly, mark the task terminally quarantined, ensure the retry/lease logic never re-dispatches it, and still emit an attempt-scoped report so a re-run isn't dedup-swallowed. But the exact integration points are exactly what I can't see.

One process note, since the brief establishes this vocabulary: this dispatch arrived without the `{task_ref, prompt, deadline_ms, session, budget, mode, attempt}` envelope, and the lane has no working tree. That reads as a lane/environment failure before any work — `infra_failed` (net-zero retry), not `work_failed`. I'm flagging it rather than self-reporting `done` or silently burning an attempt.

Tell me how you want to supply the repo (or authorize a greenfield scaffold with a named language and interface), and I'll implement it.
--- ARTIFACT_REFS ---
(none)
--- TELEMETRY ---
{
  "turns": 7,
  "wall_ms": 48584,
  "lane_attempts_used": 1,
  "lanes": [
    {
      "lane": 1,
      "key_index": 1,
      "model": "deepseek/deepseek-v4.1-flash",
      "rc": 0,
      "signal": null,
      "duration_ms": 48477,
      "wall_killed": false,
      "class": "done"
    }
  ]
}
