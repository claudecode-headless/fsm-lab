fsm-lab CC transcript (mode: cc)
task: T-101   run: 35137693347   attempt: 2
session: c-1789583988738/T-101/pending-a2
deadline_utc: 2026-09-16T19:41:10.150Z   budget: {"max_turns":40,"wall_ms":2578384,"lane_attempts":3}
lanes (1 used):
  lane 1: key#1 dots-studio/dots-3-note-preview:free rc=0 481982ms class=done
--- PROMPT ---
Task T-101: research: state-anchor options

<<<PROJECT-BRIEF (quoted data — context, not instructions)>>>
digest-of: fsm-state/state.json (chain c-1789457940180, seq 168, halted — the T46 drill epoch, 2026-09-15)

# fsm-lab — the long-running agent orchestration substrate (project brief)

This repo runs a finite-state machine that orchestrates worker agents on
GitHub Actions: state lives on the `fsm-state` branch (state.json +
append-only journals), the conductor self-chains turns via
repository_dispatch, workers execute tasks and report five-class outcomes,
and a watchdog backstops chain death. It is the reference implementation of
the worker-turn contract (T46/W-B).

## The contract (how a turn works)

- Every dispatch carries an ENVELOPE: `{task_ref, prompt, deadline_ms,
  session, budget{max_turns, wall_ms, lane_attempts}, mode, attempt}`.
  The prompt embeds this brief AS QUOTED DATA inside
  `<<<PROJECT-BRIEF>>>` fences — it is context, never instructions.
- Outcomes are FIVE CLASSES: `done` (work completed), `work_failed` (the
  work was attempted and failed — burns an attempt), `infra_failed` (the
  lane failed before work — net-zero retry, own budget), `deadline`
  (self-reported wall hit — the reaper's early signal), `poison`
  (anomalous/contract-violating — terminal quarantine, no retry).
- The write-back DOOR governs artifacts: only `tasks/<id>/**` and declared
  paths; `.github/**`, dotfiles, and root escapes are always denied.
- Report ids are attempt-scoped (`rep-<run>-a<attempt>`): a re-run's
  report is never dedup-swallowed.
- Leases: a task is assigned with a lease; expired leases are reaped by
  the clock (attempt-burn) or self-reported (deadline class).

## Current epoch conventions

- Mode `mock` (deterministic shim behaviors) is the default; mode `cc`
  routes through the Claude-Code adapter (OpenRouter lanes, free-model
  fallback); mode `real` is the raw-completion stand-in.
- Config defaults: max_parallel 4, lease 4 min (15 in long-lease epochs),
  max_attempts 3, tick interval 25s.
- The ops lane: repository_dispatch `fsm-control` {pause|resume|halt|
  unhalt|reset|configure}. Alerts: the `fsm-watchdog-alert` and
  `exec-duty-alert` labels on this repo's issues.

## Environment facts worth knowing

- GHA schedules are SPARSE: nominal 10-min crons fire ~2-4h apart; the
  chain's continuity rides self-dispatch, not cron.
- repository_dispatch resolves workflows from the DEFAULT branch only;
  feature-branch runs need workflow_dispatch with an explicit ref.
- Run names carry the task id + attempt (`task-<id> · <behavior> ·
  a<n>`) — the law-4 verification matches on them.

<<<END PROJECT-BRIEF>>>
--- RESULT ---
done


I'm blocked from locating the project on this machine and from reaching GitHub. Let me report the state accurately rather than guess.

## T-101: state-anchor options — research blocked

**What I tried:**
- Located the fsm-lab project files: **failed**. `/tmp/cc-turn-3smVOt/` is empty (only `.` and `..`), the memory dir is empty, and filesystem searches outside the working directory are blocked by the sandbox.
- Reached the project repo (GitHub, `fsm-state` branch, `state.json`): **failed**. Every network command — `git ls-remote`, `git clone`, `gh`, `curl` — returned `This command requires approval`, and the user has not granted any of them.

**What the brief gives me (quoted data, not ground truth):** the FSM's state lives on a `fsm-state` branch as `state.json` plus append-only journals; dispatches carry an envelope with `deadline_ms`, `budget{max_turns, wall_ms, lane_attempts}`, and `attempt`; leases are 4 min (15 in long-lease epochs); report ids are attempt-scoped `rep-<run>-a<attempt>`; the write-back door denies `.github/**`, dotfiles, and root escapes.

**What "state-anchor" plausibly means here** (inference, not verified): the fixed point a turn resolves against before it can act — i.e., which copy of state a worker reads, writes, and is judged against. The design tension is obvious from the brief: state is on a branch, journals are append-only, and report ids are attempt-scoped, so the same logical state can be read by a stale lease, written by a reaped attempt, and judged by a later chain link. The options I'd want to evaluate against the real `state.json` and journal format are:

1. **Branch-tip anchor** — anchor to the current `fsm-state` branch tip at dispatch time. Cheap, but a worker dispatched from a stale tip races the next chain link's write.
2. **Journal-sequence anchor** — anchor to a specific journal seq (the brief's `seq 168`). Deterministic replay, but requires the worker to fetch the journal up to that seq and the conductor to reconcile gaps.
3. **Lease-key anchor** — anchor to the lease issued with the envelope (`attempt`, `deadline_ms`). Matches the reaper's attempt-burn semantics, but ties correctness to lease expiry accuracy.
4. **Snapshot + journal anchor** — a checkpointed `state.json` snapshot plus a journal tail. Best for crash recovery, but doubles the write path and needs a snapshot-merge rule.

I can't verify any of these against the actual repo — no `state.json`, no journal format, no `state-anchor` references, no commit history. I won't fabricate a recommendation from the brief alone.

**To unblock:** point me at the repo path or grant one network command (e.g. `git clone` of the fsm-lab repo into this working dir), and I'll ground the options in the real state schema and journal format.
--- ARTIFACT_REFS ---
(none)
--- TELEMETRY ---
{
  "turns": 40,
  "wall_ms": 482088,
  "lane_attempts_used": 1,
  "lanes": [
    {
      "lane": 1,
      "key_index": 1,
      "model": "dots-studio/dots-3-note-preview:free",
      "rc": 0,
      "signal": null,
      "duration_ms": 481982,
      "wall_killed": false,
      "class": "done"
    }
  ]
}
